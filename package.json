{
  "name": "is-funcs",
  "version": "0.1.0",
  "description": "A very limited subset of type checking functions I use every day",
  "repository": "jeromedecoster/is-funcs",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "keywords": [
    "type",
    "check"
  ],
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "budo src/index.js:bundle.js --dir src --host localhost --port 3000",
    "dev": "chokidar 'index.js' 'tests/*.js' -c 'npm run test'",
    "test": "tape tests/*.js | tap-spec || true"
  },
  "devDependencies": {
    "budo": "^8.2.2",
    "chokidar-cli": "^1.2.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  }
}
